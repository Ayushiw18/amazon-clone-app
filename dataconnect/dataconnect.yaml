specVersion: "v1beta"
serviceId: "amazon-clone-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "amazon-clone-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
